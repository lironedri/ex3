//
// Created by liron on 12/16/18.
//

#include "Lexer.h"
#include <fstream>

vector<string> Lexer:: doLexer (string fileName){
    ifstream myFile(fileName);
    if (myFile.is_open()) {
        while (!myFile.eof()) {
            c
        }
}